apiVersion: apps/v1
kind: Deployment
metadata:
  name: rwb-site
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rwb-site
  template:
    metadata:
      labels:
        app: rwb-site
    spec:
      containers:
        - name: rwb-site
          image: ghcr.io/YOUR_GITHUB_USER/rwb-site:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: rwb-site
spec:
  selector:
    app: rwb-site
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
